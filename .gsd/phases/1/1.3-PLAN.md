---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: ナビゲーション機能の実装

## Objective
サイドバーにナビゲーションボタン（戻る、進む、リロード）を配置し、実際に WebView を操作できるようにします。

## Context
- src-tauri/src/lib.rs
- src/App.tsx
- .gsd/DECISIONS.md (ADR-004)

## Tasks

<task type="auto">
  <name>ナビゲーションコマンドの実装</name>
  <files>
    - src-tauri/src/lib.rs
  </files>
  <action>
    1. 前のプランで作成したスタブを実装。
    2. `navigate`: `webview.navigate(url)` を呼び出す。
    3. `go_back` / `go_forward` / `reload`: `webview.eval()` を使用して JS API (`history.back()` 等) を実行。
  </action>
  <verify>Frontend から invoke で呼び出し、WebView 内のページが期待通りに遷移・リロードすることを確認。</verify>
  <done>Rust 側の全ナビゲーションコマンドが正しく動作すること。</done>
</task>

<task type="auto">
  <name>サイドバー UI の実装</name>
  <files>
    - src/App.tsx
    - src/components/Sidebar.tsx (新規作成)
  </files>
  <action>
    1. `src/components/Sidebar.tsx` を作成。
    2. 「戻る」「進む」「更新」のボタンを上部に配置。
    3. 各ボタンの onClick イベントで対応する Tauri コマンドを invoke。
    4. App.tsx で Sidebar コンポーネントを使用し、レイアウトを整える。
  </action>
  <verify>サイドバーのボタンをクリックして、WebView の操作ができることを確認。</verify>
  <done>ナビゲーション操作（戻る・進む・更新）がサイドバーから可能になること。</done>
</task>

## Success Criteria
- [ ] サイドバーに「戻る」「進む」「更新」ボタンが表示されている。
- [ ] 各ボタンをクリックすると WebView 内のページが正しく操作される。
