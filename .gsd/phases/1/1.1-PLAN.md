---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: 基本レイアウトと通信基盤

## Objective
ブラウザの基本となる 2 カラムレイアウト（サイドバー + コンテンツ領域）を実装し、Rust 側でブラウザ操作を受け付けるためのコマンド定義を行います。

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md (ADR-004)
- .gsd/phases/1/RESEARCH.md

## Tasks

<task type="auto">
  <name>SolidJS レイアウトの実装</name>
  <files>
    - src/App.tsx
    - src/App.css
  </files>
  <action>
    1. index.css (または App.css) に基本的なリセットスタイルとレイアウト用のスタイルを追加。
    2. App.tsx を修正し、左側に幅 200px の固定サイドバー、右側に WebView が配置される予定の空領域（コンテンツ領域）を持つ Flexbox レイアウトを作成。
    3. サイドバーは背景色を持たせ、WebView 領域は透明、またはプレースホルダーを表示。
  </action>
  <verify>bun run dev で UI を表示し、サイドバーが 200px で固定されていることを確認。</verify>
  <done>サイドバーとメイン領域が左右に分かれたレイアウトがブラウザで確認できること。</done>
</task>

<task type="auto">
  <name>Rust コマンドのボイラープレート作成</name>
  <files>
    - src-tauri/src/lib.rs
  </files>
  <action>
    1. 既存の `greet` コマンドを削除（不要なため）。
    2. 以下のブラウザ操作用コマンドのスタブを Rust 側に作成。
       - `create_webview(url: String)`
       - `navigate(url: String)`
       - `go_back()`
       - `go_forward()`
       - `reload()`
    3. Tauri の `invoke_handler` にこれらのコマンドを登録。
  </action>
  <verify>Cargo check でコンパイルが通ることを確認。</verify>
  <done>指定された 5 つのコマンドが Rust 側で定義され、IPC 経由で呼び出せる状態になっていること。</done>
</task>

## Success Criteria
- [ ] サイドバー（200px）を持つ基本的な UI レイアウトが完成している。
- [ ] Rust 側にブラウザ操作用コマンドが定義されている。
